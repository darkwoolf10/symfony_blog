<h4 class="list-group-item active">Categories</h4>
{% for category in categories %}
    <a href="{{ path('category', {'categoryId': category.id }) }}" class="list-group-item list-group-item-action">
        {{ category.name }}
    </a>
{% endfor %}
